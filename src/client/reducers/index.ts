import annotation from './annotation';
import letter from './letter';
import user from './user';
import message from './message';
import search from './search';

export default { annotation, letter, message, search, user };
